[manifest]
version = "1.0.0"
dump_lua = true
priority = 0

# What even is a latios bro
[[patches]]
[patches.pattern]
target = "functions/common_events.lua"
pattern = '''
        G.GAME.hands[hand].mult = math.max(G.GAME.hands[hand].s_mult + G.GAME.hands[hand].l_mult*(G.GAME.hands[hand].level - 1), 1)
'''
position = "at"
payload = '''
if next(SMODS.find_card('j_wowzas_latios')) then
    G.GAME.hands[hand].mult = math.max(G.GAME.hands[hand].s_mult + G.GAME.hands[hand].l_mult*(G.GAME.hands[hand].level - 1) + SMODS.find_card('j_wowzas_latios')[1].ability.extra.mult_mod, 1) 
elseif next(SMODS.find_card('j_wowzas_mega_latios')) then
    G.GAME.hands[hand].mult = math.max(G.GAME.hands[hand].s_mult + G.GAME.hands[hand].l_mult*(G.GAME.hands[hand].level - 1) + SMODS.find_card('j_wowzas_mega_latios')[1].ability.extra.mult_mod, 1)
G.GAME.hands[hand].mult = math.max(G.GAME.hands[hand].s_mult + G.GAME.hands[hand].l_mult*(G.GAME.hands[hand].level - 1), 1)
end
'''
match_indent = true

# Okay so latias works too
[[patches]]
[patches.pattern]
target = "functions/common_events.lua"
pattern = '''
        G.GAME.hands[hand].chips = math.max(G.GAME.hands[hand].s_chips + G.GAME.hands[hand].l_chips*(G.GAME.hands[hand].level - 1), 0)
'''
position = "at"
payload = '''
if next(SMODS.find_card('j_wowzas_latias')) then
    G.GAME.hands[hand].chips = math.max(G.GAME.hands[hand].s_chips + G.GAME.hands[hand].l_chips*(G.GAME.hands[hand].level - 1) + SMODS.find_card('j_wowzas_latias')[1].ability.extra.chips, 0) 
elseif next(SMODS.find_card('j_wowzas_mega_latias')) then
    G.GAME.hands[hand].chips = math.max(G.GAME.hands[hand].s_chips + G.GAME.hands[hand].l_chips*(G.GAME.hands[hand].level - 1) + SMODS.find_card('j_wowzas_mega_latias')[1].ability.extra.chips, 0)
else
G.GAME.hands[hand].mult = math.max(G.GAME.hands[hand].s_mult + G.GAME.hands[hand].l_mult*(G.GAME.hands[hand].level - 1), 1)
end
'''
match_indent = true